cabal sandbox init
cabal sandbox add-source ../a-parser
cabal install


──── Пакеты, которые ставил для работы с gtk2

x11-libs/gtk+-2.x

dev-util/glade-3.8.5
   Графический редактор для разработки интерфейсов.
   Последняя из доступных версий, поддерживающая gtk2.

dev-python/xlrd

dev-haskell/gtk2hs-buildtools
   Это нужно во время сборки dev-haskell/gtk, но не уверен, что я ставил это 
   руками. Скорее всего тут всё правильно.


──── Не актуально

dev-haskell/gtk-0.12.4-r1
   Хаскельные бинды к gtk.
   Ставлю последнюю из доступных не замаскированных версий, которая 
      поддерживает gtk2. Версия 3.18.3 уже не поддерживает gtk2, а 
      поддерживает gtk3.

dev-haskell/glade — Binding to the glade library
   Без этой либы не получится считать из хаскеля glade-файл.
